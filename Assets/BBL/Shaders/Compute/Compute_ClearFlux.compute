#pragma kernel ClearFlux

#include "Assets/BBL/Shaders/ShaderIncludes/Karst/Simulation/Flux.hlsl"

int _MaxVoxels;

[numthreads(32,1,1)]
void ClearFlux (uint3 id : SV_DispatchThreadID)
{
    if (id.x >= _MaxVoxels)
        return;

    _FluxBuffer[id.x] = (Flux)0;
}